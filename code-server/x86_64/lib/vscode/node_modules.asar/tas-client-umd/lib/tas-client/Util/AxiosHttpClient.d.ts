import { AxiosRequestConfig, AxiosPromise } from 'axios';
export declare class AxiosHttpClient {
    private endpoint;
    constructor(endpoint: string);
    get(config?: AxiosRequestConfig | undefined): AxiosPromise<any>;
}
